apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: db
    type: back-tier
spec:
  type: ClusterIP
  ports:
    - name: "db-service"
      port: 5432
      targetPort: 5432
  selector:
    app: db
    type: back-tier
